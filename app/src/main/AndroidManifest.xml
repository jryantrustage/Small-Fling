<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Essential Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- MediaProjection & Foreground Service Requirements (Android 14+ / 15 / 16) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Storage / Export Output -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" tools:ignore="ScopedStorage" />

    <application
        android:name=".MatrixCaptureApp"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="MatrixCapture"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- Main Controller Activity (Runs on Screen 0 / Phone Display) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="false"
            android:lockTaskMode="if_whitelisted"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Module B: Desktop Pagination Accessibility Service (Display 1 Automation) -->
        <service
            android:name=".service.DesktopPaginationService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Module C: Foreground Recording & Frame Ingestion Service -->
        <service
            android:name=".service.SegmentRecorderService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Floating HUD Overlay Service (Display 0 or Display 1 real-time telemetry) -->
        <service
            android:name=".service.FloatingOverlayService"
            android:exported="false" />

        <!-- Dedicated External Kiosk Mode Presentation Activity -->
        <activity
            android:name=".kiosk.KioskPresentationActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="false"
            android:lockTaskMode="if_whitelisted"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Kiosk Device Admin / DPC Receiver -->
        <receiver
            android:name=".kiosk.KioskAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_kiosk" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.LOCK_TASK_ENTERING" />
                <action android:name="android.app.action.LOCK_TASK_EXITING" />
            </intent-filter>
        </receiver>

        <!-- Kiosk RPC Command Receiver -->
        <receiver
            android:name=".kiosk.KioskCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.matrixcapture.app.action.LOCK_EXTERNAL_DISPLAY" />
                <action android:name="com.matrixcapture.app.action.RELEASE_LOCK" />
                <action android:name="com.matrixcapture.app.action.QUERY_STATUS" />
                <action android:name="com.matrixcapture.app.action.AUTO_REFRESH_DISPLAY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
